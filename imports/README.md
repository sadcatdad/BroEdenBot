Place local DiscordChatExporter exports here. These files are ignored and
should not be committed.

Historical channel exports belong in `imports/discord_history/`.

Full-server CSV exports for private `/context` plus safe missing-channel
activity backfill belong in `imports/message_context/`.
